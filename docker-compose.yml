version: "3.8"

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: universal_rag_qdrant
    ports:
      - "6333:6333"      # REST API
      - "6334:6334"      # gRPC
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: always

volumes:
  qdrant_storage:
